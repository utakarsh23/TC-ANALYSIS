# Environment Variables

# Development (Local)
# PORT=9092
# USE_DOCKER=false
# DOCKER_IMAGE=dsa-runtime:latest
# CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# Production (Render with Code Execution)
PORT=9092
NODE_ENV=production
USE_DOCKER=false
CORS_ORIGIN=https://your-vercel-domain.vercel.app
# Code execution happens locally in the container (compilers pre-installed)
# Java, C++, Python are all available
